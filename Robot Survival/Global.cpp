#include "Global.h"

namespace global
{
	const unsigned short windowWidth = 1024;
	const unsigned short windowHeight = 768;
	sf::Vector2i mousePosInWindow;
	sf::Vector2f windowCenter(windowWidth / 2.f, windowHeight / 2.f);
}